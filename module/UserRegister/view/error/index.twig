{% extends "layout/layout.twig" %}
{% block title %}500 Internal Server Error.{% endblock %}

{% block sideMenu '' %}

{% block container %}
<h2 class="title">500 Internal Server Error.</h2>
<table class="regster-form-table">
<tr>
    <td>
        <p>お探しのページは表示できません。</p>
        <p>システムエラーが発生しております。お手数ですが、しばらく時間をおいてから再度お試しください</p>
        <a href="{{ basepath() }}/menu">トップページへ戻る</a>
    </td>
</tr>
</table>

{# 本番(production)以外には詳細なエラーメッセージを表示する #}
{% if not env.isProduction() %}
<h1>An error occurred</h1>
<h2>{{ message }}</h2>
<h2>Additional information:</h2>
<dl>
    <dt>File:</dt>
    <dd>
        <pre>{{ exception.getFile() }}:{{ exception.getLine() }}</pre>
    </dd>
    <dt>Message:</dt>
    <dd>
        <pre>{{ escapeHtml(exception.getMessage()) }}</pre>
    </dd>
    <dt>Stack trace:</dt>
    <dd>
        <pre>{{ escapeHtml(exception.getTraceAsString()) }}</pre>
    </dd>
</dl>
{% endif %}
{% endblock %}
